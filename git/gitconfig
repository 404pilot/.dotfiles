[user]
    name = neatpilot
    email = neatpilot@users.noreply.github.com

[color]
    ui = true

[alias]
    co = checkout
    st = status
    br = branch -a
    cmm = commit -m
    lol = log --oneline -n 10
    bye = reset --hard HEAD
    wormhole = reset --hard HEAD~1
    sh = stash
    mg = merge --no-ff

[push]
    # default = simple
    default = current