# Keymap

####`Main menu`>`Window`>`Editor Tabs`

`close` : alt + w


####`Main menu`>`Navigate`

`select in ...` alt + c

`Back` alt + a

`Forward` alt + d

# Editor tab

#### Show all tabs
`Editor`>`General`>`Editor Tabs` : uncheck `Show tabs in single row`
