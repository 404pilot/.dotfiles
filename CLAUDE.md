# Dotfiles project rules

## Applying changes

Never apply dotfile changes directly (manual `ln -s`, writing config files, installing packages, etc.). Always run `bootstrap.sh` to apply changes. Check the script for available flags.
